{%
  set classes = [
  'node',
  'node--type-' ~ node.bundle|clean_class,
  node.isPromoted() ? 'node--promoted',
  node.isSticky() ? 'node--sticky',
  not node.isPublished() ? 'node--unpublished',
  view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
]
%}
{{ attach_library('classy/node') }}
<article{{ attributes.addClass(classes) }}>



{#  {% if display_submitted %}#}
{#    <footer class="node__meta">#}
{#      {{ author_picture }}#}
{#      <div{{ author_attributes.addClass('node__submitted') }}>#}
{#        {% trans %}Submitted by {{ author_name }} on {{ date }}{% endtrans %}#}
{#        {{ metadata }}#}
{#      </div>#}
{#    </footer>#}
{#  {% endif %}#}

  <div{{ content_attributes.addClass('node__content') }}>
    <a class="cover-link" href="{{ url }}">
      <div class="covered-div">
        <div class="every-image-banner">
          {{ content.field_image_banner }}
        </div>
        <div class="every-image-title">
          {{ title_prefix }}
          {% if label and not page %}
            <h2{{ title_attributes }}>{{ label }}</h2>
          {% endif %}
          {{ title_suffix }}
        </div>
          {% if content.field_content_paragraph %}
            <div class="every-news-summary">{{ content.field_content_paragraph }}</div>
          {% endif %}
      </div>
    </a>


{#    {% if content.field_category %}#}
{#      <div class="category-{{ content.field_category[0]['#title']|lower }}">{{ content.field_category }}</div>#}
{#    {% endif %}#}
{#  </div>#}

</article>
